{
  "name": "@sinapsis-co/cloud-components",
  "description": "Developer utils",
  "author": "sinapsis.co",
  "license": "MIT",
  "repository": "git://github.com/sinapsis-co/co.sinapsis.cloud-components.git",
  "private": true,
  "workspaces": [
    "platform",
    "infra"
  ],
  "scripts": {
    "verify": "npx tsc --pretty --noEmit && npx eslint . --ext .ts && npx prettier -c . && npx cspell '**/*.ts'",
    "release": "bash script/release.bash",
    "deploy": "sh -c 'yarn --cwd templates deploy ${0}'"
  },
  "lint-staged": {
    "*.ts": [
      "tsc --pretty --noEmit --esModuleInterop",
      "eslint . --ext .ts",
      "prettier --write",
      "npx cspell '**/*.ts'"
    ],
    "*.json": "prettier --write",
    "*.js": "eslint --cache --fix",
    "*.{js,css,md}": "prettier --write"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.83",
    "@types/node": "^17.0.21",
    "@typescript-eslint/eslint-plugin": "^4.28.2",
    "@typescript-eslint/parser": "^4.28.2",
    "aws-sdk": "^2.1209.0",
    "cspell": "^5.13.3",
    "eslint": "^7.30.0",
    "husky": "^7.0.4",
    "lint-staged": ">=10",
    "prettier": "^2.3.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.3.5"
  }
}
